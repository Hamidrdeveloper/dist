import { DefaultTheme } from 'styled-components';

export interface CompanyThemesFullModel {
  id: 18;
  slug: 'theme';
  data: DefaultTheme;
  company_id: null | number;
  partner_id: null | number;
  subdomain_id: null | number;
}
